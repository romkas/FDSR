#include "compute.h"
#include <numeric>
#include <iterator>
using namespace model;


float model::compute(const std::vector<cv::Vec3f*>::iterator it_start,
    const std::vector<cv::Vec3f*>::iterator it_end,
    Plane * M, GradientDescent * E)
{

}